import streamlit as st


def display_image_features():

    st.write(
        """
    Image-based EDA will be completed and added after the ML experiments and before the
    DL experiments.

    Since I am not planning to use images during the ML experiments, the focus for now
    is on tabular and text features.
    """
    )
